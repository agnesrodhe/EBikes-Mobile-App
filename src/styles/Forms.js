import { StyleSheet } from 'react-native';

export const forms = StyleSheet.create({
    form: {
      width: '100%',
      fontSize: 20,
      borderWidth: 1,
      padding: 10,
      borderColor: "#ccc",
      borderRadius: 7,
      borderColor: "black",
      backgroundColor: '#fff'
    }
});